steps:
  - label: ':hammer_and_wrench: Run full test suite'
    command: './scripts/buildkite-test.sh'
    artifact_paths: '*.buildkite.log'
    agents:
      docker: 'true'
    plugins:
      docker-compose#v1.3.2:
        run: build
        config: docker-compose-build.yml