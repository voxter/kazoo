{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "_id": "kapi.pivot.succeeded",
    "description": "AMQP API for pivot.succeeded",
    "properties": {
        "Call-ID": {
            "type": "string"
        },
        "Event-Category": {
            "enum": [
                "pivot"
            ],
            "type": "string"
        },
        "Event-Name": {
            "enum": [
                "succeeded"
            ],
            "type": "string"
        }
    },
    "required": [
        "Call-ID"
    ],
    "type": "object"
}
