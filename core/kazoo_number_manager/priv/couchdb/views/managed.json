{
    "_id": "_design/numbers",
    "language": "javascript",
    "views": {
        "listing_undefined_module": {
            "map": "function(doc) { if (doc.pvt_module_name != 'undefined' ) return; emit([doc._id], [doc.pvt_module_name]); }"
        },
        "status": {
            "map": "function(doc) { emit([doc.pvt_account_id, (typeof doc.pvt_state !== 'undefined') ? doc.pvt_state : doc.pvt_number_state, doc._id], null); }"
        }
    }
}
