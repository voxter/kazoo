version: '3'
services:
  couchdb:
    image: couchdb:2
  rabbitmq:
    image: rabbitmq
  build:
    build:
      context: .
      dockerfile: Dockerfile.build
    domainname: local.voxter.net
    volumes:
      - .:/app
    depends_on:
      - couchdb
      - rabbitmq
